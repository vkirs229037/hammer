let a = 5;
let b;

let temp = a;
a = b;
b = temp;

println(a);
println(b);

// Этот пример отображает некорректную работу компилятора.
// Так как при построении дерева было определено, что b якобы инициализирована, 
// то строка 5 не вызывает ошибки, хотя на самом деле в тот момент времени 
// переменная b не имеет значения!